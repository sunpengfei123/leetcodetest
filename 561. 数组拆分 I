class Solution {
    public int arrayPairSum(int[] nums) {
        Arrays.sort(nums);
        int re = 0;
        for(int i = 0; i<nums.length; i+=2){
            re+=nums[i];        
        }
        return re;
    }
}
